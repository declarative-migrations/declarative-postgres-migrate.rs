[package]
org = "declarative-migrations"
name = "declarative-postgres-migrate"
version = "0.3.2"
description = "Declarative PostgreSQL and CockroachDB schema diff library and dpm command-line tool"
license = "MIT"
keywords = ["cli", "postgres", "migration", "schema", "global-tool"]
language = "rust"

[package.repository]
vcs = "git"
url = "https://github.com/declarative-migrations/declarative-postgres-migrate.rs"

[build]
command = "cargo build --release --locked"
outputs = ["target/release/dpm"]

[bin]
dpm = "target/release/dpm"

[publish]
include_readme = true
tag_format = "v{version}"
smoke_test = "test -x \"$ZED_PKG_TEST_TARGET/target/release/dpm\" && \"$ZED_PKG_TEST_TARGET/target/release/dpm\" --help >/dev/null"
exclude = [".env", ".env.*", ".direnv/**", ".zed/**", ".zed-pack/**", "target/**", "tmp/**", "**/*.log", ".DS_Store", "tests/**", "scripts/publish/**"]

[publish.native]
registry = "crates-io"
package = "declarative-postgres-migrate"

[install]
adapter = "none"

[scripts]
test = "cargo test --all-targets --locked"
