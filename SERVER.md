# dpm-server

The hardened Rust HTTP service, configuration, API examples, safety model, and
consumer guidance are documented in [`docs/server.md`](docs/server.md).

The proposed `dpm-interfaces`, `dpm-cli`, `dpm-sync`, and `dpm-server`
extraction boundaries are documented in
[`docs/repository-boundaries.md`](docs/repository-boundaries.md).
